<h1>Editing Mood</h1>
<% @mood = Mood.using(:moodshard).find_by_id(@mood.id) %>
<%= form_for(@mood) do |f| %>
  <% if @mood.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mood.errors.count, "error") %> prohibited this mood from being saved:</h2>

      <ul>
      <% @mood.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :color %><br>
    <%= f.text_field :color %>
  </div>
  <div class="field">
    <%= f.label :mood %><br>
    <%= f.text_field :mood %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<!--<%= render 'form' %>-->

<%= link_to 'Show', @mood %> |
<%= link_to 'Back', moods_path %>
